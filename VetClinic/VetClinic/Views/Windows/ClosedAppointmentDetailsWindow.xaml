<Window x:Class="VetClinic.Views.Windows.ClosedAppointmentDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Appointment Details" Height="600" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="20">
        <TextBlock Text="Appointment Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Appointment Info -->
        <TextBlock Text="{Binding Appointment.Date, StringFormat=Date: {0:d}}" Margin="0,0,0,5"/>
        <TextBlock Text="{Binding Appointment.Service.Name, StringFormat=Service: {0}}" Margin="0,0,0,5"/>

        <Separator Margin="0,10"/>

        <!-- Pet Info -->
        <TextBlock Text="{Binding Appointment.Pet.Name, StringFormat=Pet: {0}}" Margin="0,0,0,5"/>
        <TextBlock Text="{Binding Appointment.Pet.Species, StringFormat=Species: {0}}" Margin="0,0,0,5"/>
        <TextBlock Text="{Binding Appointment.Pet.Breed, StringFormat=Breed: {0}}" Margin="0,0,0,5"/>

        <Separator Margin="0,10"/>

        <!-- Owner Info -->
        <TextBlock Text="{Binding Appointment.Pet.Owner.Name, StringFormat=Owner: {0}}" Margin="0,0,0,5"/>
        <TextBlock Text="{Binding Appointment.Pet.Owner.Email, StringFormat=Email: {0}}" Margin="0,0,0,5"/>

        <Separator Margin="0,10"/>

        <!-- Medical Record Info -->
        <TextBlock Text="Medical Record" FontWeight="Bold" FontSize="16" Margin="0,10,0,10"/>

        <TextBlock Text="{Binding MedicalRecord.Diagnosis, StringFormat=Diagnosis: {0}}" Margin="0,0,0,5"/>
        <TextBlock Text="{Binding MedicalRecord.Treatment, StringFormat=Treatment: {0}}" Margin="0,0,0,5"/>
        <TextBlock Text="{Binding MedicalRecord.Medications, StringFormat=Medications: {0}}" Margin="0,0,0,5"/>
        <TextBlock Text="{Binding MedicalRecord.Notes, StringFormat=Notes: {0}}" TextWrapping="Wrap" Margin="0,0,0,5"/>

        <Button Content="Close" Click="Close_Click" HorizontalAlignment="Right" Margin="0,20,0,0"/>
    </StackPanel>
</Window>
